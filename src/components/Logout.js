import React from 'react';

function Logout() {
  return (
    <div className="container">
      <h2>Logging out...</h2>
    </div>
  );
}
// Logout.propTypes = {};
// Logout.defaultProps = {};

export default Logout;
